"""Shared helpers: data classes, plot styling, colors, paths, argument parsing.

Submodules are deliberately not re-exported here so that importing a light
helper (e.g. ``path_utils``) does not pull in matplotlib or plotly.
"""
